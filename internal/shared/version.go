// Package shared contains code reused by both the account-api and
// account-webhook services (Stripe client wrapper, common models, errors).
package shared

// Version is the current build version of the billing-engine module.
const Version = "0.0.0"
