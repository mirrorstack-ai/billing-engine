// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: standing.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const accountOwnerByStripeCustomer = `-- name: AccountOwnerByStripeCustomer :one

SELECT owner_user_id, owner_org_id
FROM ms_billing.accounts
WHERE stripe_customer_id = $1
`

type AccountOwnerByStripeCustomerRow struct {
	OwnerUserID pgtype.UUID `json:"owner_user_id"`
	OwnerOrgID  pgtype.UUID `json:"owner_org_id"`
}

// Owner-principal lookups for the serving-block standing notifier
// (internal/account/standing): a webhook event carries a Stripe object id
// (customer / invoice / payment method), and the notifier must resolve the
// OWNER (user XOR org) whose standing may have transitioned so it can POST
// the current blocked verdict to api-platform's /internal/apps/serving-block.
// All three are read-only projections of the accounts row's owner columns;
// exactly one of owner_user_id / owner_org_id is non-NULL per the accounts
// polymorphic-owner CHECK (001/041).
func (q *Queries) AccountOwnerByStripeCustomer(ctx context.Context, stripeCustomerID pgtype.Text) (AccountOwnerByStripeCustomerRow, error) {
	row := q.db.QueryRow(ctx, accountOwnerByStripeCustomer, stripeCustomerID)
	var i AccountOwnerByStripeCustomerRow
	err := row.Scan(&i.OwnerUserID, &i.OwnerOrgID)
	return i, err
}

const accountOwnerByStripeInvoice = `-- name: AccountOwnerByStripeInvoice :one
SELECT a.owner_user_id, a.owner_org_id
FROM ms_billing.invoices i
JOIN ms_billing.accounts a ON a.id = i.account_id
WHERE i.stripe_invoice_id = $1
`

type AccountOwnerByStripeInvoiceRow struct {
	OwnerUserID pgtype.UUID `json:"owner_user_id"`
	OwnerOrgID  pgtype.UUID `json:"owner_org_id"`
}

func (q *Queries) AccountOwnerByStripeInvoice(ctx context.Context, stripeInvoiceID string) (AccountOwnerByStripeInvoiceRow, error) {
	row := q.db.QueryRow(ctx, accountOwnerByStripeInvoice, stripeInvoiceID)
	var i AccountOwnerByStripeInvoiceRow
	err := row.Scan(&i.OwnerUserID, &i.OwnerOrgID)
	return i, err
}

const accountOwnerByStripePaymentMethod = `-- name: AccountOwnerByStripePaymentMethod :one
SELECT a.owner_user_id, a.owner_org_id
FROM ms_billing.payment_methods_mirror pm
JOIN ms_billing.accounts a ON a.id = pm.account_id
WHERE pm.stripe_payment_method_id = $1
`

type AccountOwnerByStripePaymentMethodRow struct {
	OwnerUserID pgtype.UUID `json:"owner_user_id"`
	OwnerOrgID  pgtype.UUID `json:"owner_org_id"`
}

func (q *Queries) AccountOwnerByStripePaymentMethod(ctx context.Context, stripePaymentMethodID string) (AccountOwnerByStripePaymentMethodRow, error) {
	row := q.db.QueryRow(ctx, accountOwnerByStripePaymentMethod, stripePaymentMethodID)
	var i AccountOwnerByStripePaymentMethodRow
	err := row.Scan(&i.OwnerUserID, &i.OwnerOrgID)
	return i, err
}
