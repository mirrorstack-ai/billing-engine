// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: org_deletion.sql

package db

import (
	"context"
	"time"

	"github.com/jackc/pgx/v5/pgtype"
)

const acquireOrgBillingLifecycleLock = `-- name: AcquireOrgBillingLifecycleLock :one
SELECT 1::bigint AS locked
FROM (
    SELECT pg_advisory_xact_lock(
        hashtextextended('ms_billing.org.lifecycle:' || CAST($1::uuid AS text), 0)
    )
) AS lifecycle_lock
`

// AcquireOrgBillingLifecycleLock serializes finalization with every trigger-
// guarded future org-billing write.  The string namespace must remain byte-
// identical to assert_org_billing_active in migration 052.
func (q *Queries) AcquireOrgBillingLifecycleLock(ctx context.Context, orgID string) (int64, error) {
	row := q.db.QueryRow(ctx, acquireOrgBillingLifecycleLock, orgID)
	var locked int64
	err := row.Scan(&locked)
	return locked, err
}

const assertOrgBillingWriterPairActive = `-- name: AssertOrgBillingWriterPairActive :one
SELECT 1::bigint AS active
FROM (
    SELECT ms_billing.assert_org_billing_active_pair(
        $1::uuid,
        $2::uuid
    )
) AS lifecycle_guard
`

type AssertOrgBillingWriterPairActiveParams struct {
	FirstOrgID  string `json:"first_org_id"`
	SecondOrgID string `json:"second_org_id"`
}

// AssertOrgBillingWriterPairActive acquires shared lifecycle locks in stable
// UUID order and rejects either retired side. Distributor-authorized writes
// call this and re-read the live designation in the same transaction.
func (q *Queries) AssertOrgBillingWriterPairActive(ctx context.Context, arg AssertOrgBillingWriterPairActiveParams) (int64, error) {
	row := q.db.QueryRow(ctx, assertOrgBillingWriterPairActive, arg.FirstOrgID, arg.SecondOrgID)
	var active int64
	err := row.Scan(&active)
	return active, err
}

const deleteOrgDesignationForFinalization = `-- name: DeleteOrgDesignationForFinalization :execrows
DELETE FROM ms_billing.org_billing_designations
WHERE org_id = $1
`

func (q *Queries) DeleteOrgDesignationForFinalization(ctx context.Context, orgID string) (int64, error) {
	result, err := q.db.Exec(ctx, deleteOrgDesignationForFinalization, orgID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const deleteOrgOutboundSponsorships = `-- name: DeleteOrgOutboundSponsorships :execrows
DELETE FROM ms_billing.org_billing_designations
WHERE sponsor_account_id IN (
    SELECT id
    FROM ms_billing.accounts
    WHERE owner_kind = 'org' AND owner_org_id = $1::uuid
)
`

// A first-party/distributor org account can sponsor other organizations.
// Retiring it must remove those future funding edges too; the customer org
// accounts and every historical financial row remain intact.
func (q *Queries) DeleteOrgOutboundSponsorships(ctx context.Context, orgID string) (int64, error) {
	result, err := q.db.Exec(ctx, deleteOrgOutboundSponsorships, orgID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const disableOrgAutoTopUp = `-- name: DisableOrgAutoTopUp :execrows
UPDATE ms_billing.credit_auto_topup_configs config
SET enabled = false,
    payment_method_id = NULL
FROM ms_billing.accounts account
WHERE config.account_id = account.id
  AND account.owner_kind = 'org'
  AND account.owner_org_id = $1::uuid
`

func (q *Queries) DisableOrgAutoTopUp(ctx context.Context, orgID string) (int64, error) {
	result, err := q.db.Exec(ctx, disableOrgAutoTopUp, orgID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const failOrgPendingAddCardRequests = `-- name: FailOrgPendingAddCardRequests :execrows
UPDATE ms_billing.add_card_requests request
SET status = 'failed',
    resolved_at = COALESCE(request.resolved_at, $1::timestamptz)
FROM ms_billing.accounts account
WHERE request.account_id = account.id
  AND account.owner_kind = 'org'
  AND account.owner_org_id = $2::uuid
  AND request.status = 'pending'
`

type FailOrgPendingAddCardRequestsParams struct {
	FinalizedAt time.Time `json:"finalized_at"`
	OrgID       string    `json:"org_id"`
}

// A pending SetupIntent may still deliver webhooks later.  Making its local
// request terminal prevents that replay from resolving a deleted org's card.
func (q *Queries) FailOrgPendingAddCardRequests(ctx context.Context, arg FailOrgPendingAddCardRequestsParams) (int64, error) {
	result, err := q.db.Exec(ctx, failOrgPendingAddCardRequests, arg.FinalizedAt, arg.OrgID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const finalCollectibleOrgInvoiceCount = `-- name: FinalCollectibleOrgInvoiceCount :one
WITH direct_accounts AS MATERIALIZED (
    SELECT id
    FROM ms_billing.accounts
    WHERE owner_kind = 'org' AND owner_org_id = $1::uuid
)
SELECT count(*)::bigint AS collectible_count
FROM ms_billing.invoices invoice
WHERE invoice.status IN ('open', 'uncollectible')
  AND invoice.amount_due > 0
  AND (
        invoice.account_id IN (SELECT id FROM direct_accounts)
        OR invoice.charge_funding_account_id IN (SELECT id FROM direct_accounts)
        OR (
            invoice.charge_funding_legacy_unresolved
            AND EXISTS (SELECT 1 FROM direct_accounts)
        )
  )
`

// FinalCollectibleOrgInvoiceCount is the authoritative last check immediately
// before retirement. A collectible invoice blocks both its customer owner and
// the exact account whose Stripe Customer authorized collection. Pre-052
// invoices lack that proof, so any still-collectible quarantined invoice is a
// fail-closed global retirement barrier until operations settles or verifies
// its historical payer.
func (q *Queries) FinalCollectibleOrgInvoiceCount(ctx context.Context, orgID string) (int64, error) {
	row := q.db.QueryRow(ctx, finalCollectibleOrgInvoiceCount, orgID)
	var collectible_count int64
	err := row.Scan(&collectible_count)
	return collectible_count, err
}

const getOrgDeletionFinalizationForUpdate = `-- name: GetOrgDeletionFinalizationForUpdate :one
SELECT org_id, operation_id, finalized_at
FROM ms_billing.org_deletion_finalizations
WHERE org_id = $1
FOR UPDATE
`

func (q *Queries) GetOrgDeletionFinalizationForUpdate(ctx context.Context, orgID string) (MsBillingOrgDeletionFinalization, error) {
	row := q.db.QueryRow(ctx, getOrgDeletionFinalizationForUpdate, orgID)
	var i MsBillingOrgDeletionFinalization
	err := row.Scan(&i.OrgID, &i.OperationID, &i.FinalizedAt)
	return i, err
}

const insertOrgDeletionFinalization = `-- name: InsertOrgDeletionFinalization :exec
INSERT INTO ms_billing.org_deletion_finalizations
    (org_id, operation_id, finalized_at)
VALUES
    ($1::uuid, $2::uuid, $3::timestamptz)
`

type InsertOrgDeletionFinalizationParams struct {
	OrgID       string    `json:"org_id"`
	OperationID string    `json:"operation_id"`
	FinalizedAt time.Time `json:"finalized_at"`
}

// Inserted last, after all retirement writes.  The transaction-scoped
// lifecycle lock is still held, so no guarded writer can interleave.
func (q *Queries) InsertOrgDeletionFinalization(ctx context.Context, arg InsertOrgDeletionFinalizationParams) error {
	_, err := q.db.Exec(ctx, insertOrgDeletionFinalization, arg.OrgID, arg.OperationID, arg.FinalizedAt)
	return err
}

const listOrgAccountsForDeletion = `-- name: ListOrgAccountsForDeletion :many
SELECT id
FROM ms_billing.accounts
WHERE owner_kind = 'org' AND owner_org_id = $1
ORDER BY id
FOR UPDATE
`

// Lock every historical account owned by the organization. The schema does
// not enforce one-account-per-org, so finalization must never inspect only an
// arbitrary account and miss a collectible obligation on another.
func (q *Queries) ListOrgAccountsForDeletion(ctx context.Context, ownerOrgID pgtype.UUID) ([]string, error) {
	rows, err := q.db.Query(ctx, listOrgAccountsForDeletion, ownerOrgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var id string
		if err := rows.Scan(&id); err != nil {
			return nil, err
		}
		items = append(items, id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const orgBillingInFlightCount = `-- name: OrgBillingInFlightCount :one
WITH direct_accounts AS MATERIALIZED (
    SELECT id
    FROM ms_billing.accounts
    WHERE owner_kind = 'org' AND owner_org_id = $1::uuid
),
charge_apps AS MATERIALIZED (
    SELECT app_id
    FROM ms_billing.apps
    WHERE account_id IN (SELECT id FROM direct_accounts)
)
SELECT (
    -- Every non-invoiced run is recoverable and owns retained usage and/or a
    -- frozen Stripe request. Deletion must not bury failed/skipped recovery.
    (SELECT count(*) FROM ms_billing.billing_runs
      WHERE (
            account_id IN (SELECT id FROM direct_accounts)
            OR charge_funding_account_id IN (SELECT id FROM direct_accounts)
            OR (
                charge_funding_legacy_unresolved
                AND EXISTS (SELECT 1 FROM direct_accounts)
            )
        )
        AND status <> 'invoiced')
  + (SELECT count(*) FROM ms_billing.app_combined_proration_attempts
      WHERE (
            account_id IN (SELECT id FROM direct_accounts)
            OR charge_funding_account_id IN (SELECT id FROM direct_accounts)
            OR (
                charge_funding_legacy_unresolved
                AND EXISTS (SELECT 1 FROM direct_accounts)
            )
        )
        AND resolved_at IS NULL)
  + (SELECT count(*) FROM ms_billing.app_custom_domains
      WHERE (
            app_id IN (SELECT app_id FROM charge_apps)
            OR charge_funding_account_id IN (SELECT id FROM direct_accounts)
            OR (
                charge_funding_legacy_unresolved
                AND EXISTS (SELECT 1 FROM direct_accounts)
            )
        )
        AND charge_attempted_at IS NOT NULL AND charge_resolved = false)
  + (SELECT count(*) FROM ms_billing.app_module_overage_timers
      WHERE (
            app_id IN (SELECT app_id FROM charge_apps)
            OR charge_funding_account_id IN (SELECT id FROM direct_accounts)
            OR (
                charge_funding_legacy_unresolved
                AND EXISTS (SELECT 1 FROM direct_accounts)
            )
        )
        AND charge_attempted_at IS NOT NULL AND grace_resolved = false)
  + (SELECT count(*) FROM ms_billing.credit_ledger
      WHERE type IN ('purchase', 'auto_topup')
        AND status = 'pending'
        AND (
            account_id IN (SELECT id FROM direct_accounts)
            OR (
                type = 'purchase'
                AND charge_funding_account_id IN (SELECT id FROM direct_accounts)
            )
            OR (
                type = 'purchase'
                AND charge_funding_legacy_unresolved
                AND EXISTS (SELECT 1 FROM direct_accounts)
            )
            OR (
                type = 'auto_topup'
                AND attempt_stripe_customer_id IN (
                    SELECT stripe_customer_id
                    FROM ms_billing.accounts
                    WHERE id IN (SELECT id FROM direct_accounts)
                      AND stripe_customer_id IS NOT NULL
                )
            )
        ))
)::bigint AS in_flight_count
`

// A charge that has durably started but has not yet reached a terminal local
// state can still create a collectible Stripe invoice.  Finalization waits for
// recovery/reconciliation instead of racing that money movement.
func (q *Queries) OrgBillingInFlightCount(ctx context.Context, orgID string) (int64, error) {
	row := q.db.QueryRow(ctx, orgBillingInFlightCount, orgID)
	var in_flight_count int64
	err := row.Scan(&in_flight_count)
	return in_flight_count, err
}

const retainOrgOutboundSponsorships = `-- name: RetainOrgOutboundSponsorships :execrows
INSERT INTO ms_billing.org_deletion_retired_sponsorships (
    retired_sponsor_org_id,
    customer_org_id,
    sponsor_account_id,
    operation_id,
    retired_at
)
SELECT
    $1::uuid,
    designation.org_id,
    designation.sponsor_account_id,
    $2::uuid,
    $3::timestamptz
FROM ms_billing.org_billing_designations designation
WHERE designation.funding = 'sponsor'
  AND designation.sponsor_account_id IN (
      SELECT id
      FROM ms_billing.accounts
      WHERE owner_kind = 'org' AND owner_org_id = $1::uuid
  )
ON CONFLICT (retired_sponsor_org_id, customer_org_id) DO NOTHING
`

type RetainOrgOutboundSponsorshipsParams struct {
	OrgID       string    `json:"org_id"`
	OperationID string    `json:"operation_id"`
	RetiredAt   time.Time `json:"retired_at"`
}

// Retain the exact outbound sponsor edges before removing their live
// designations. The finalization FK is deferred because the immutable
// tombstone is deliberately inserted last in the transaction.
func (q *Queries) RetainOrgOutboundSponsorships(ctx context.Context, arg RetainOrgOutboundSponsorshipsParams) (int64, error) {
	result, err := q.db.Exec(ctx, retainOrgOutboundSponsorships, arg.OrgID, arg.OperationID, arg.RetiredAt)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const retireOrgAccountCollection = `-- name: RetireOrgAccountCollection :execrows
UPDATE ms_billing.accounts
SET usage_billing_mode = 'prepaid',
    credit_limit_micros = 0,
    spend_ceiling_micros = 0
WHERE owner_kind = 'org' AND owner_org_id = $1::uuid
`

// Retired accounts remain immutable history but are forced onto the
// no-off-session posture as an additional defence behind the tombstone.
func (q *Queries) RetireOrgAccountCollection(ctx context.Context, orgID string) (int64, error) {
	result, err := q.db.Exec(ctx, retireOrgAccountCollection, orgID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const retireOrgApps = `-- name: RetireOrgApps :execrows
UPDATE ms_billing.apps
SET deleted_at = COALESCE(deleted_at, $1::timestamptz),
    proration_skipped_at = CASE
        WHEN proration_invoice_id IS NULL
            THEN COALESCE(proration_skipped_at, $1::timestamptz)
        ELSE proration_skipped_at
    END
WHERE owner_org_id = $2::uuid
`

type RetireOrgAppsParams struct {
	FinalizedAt time.Time `json:"finalized_at"`
	OrgID       string    `json:"org_id"`
}

func (q *Queries) RetireOrgApps(ctx context.Context, arg RetireOrgAppsParams) (int64, error) {
	result, err := q.db.Exec(ctx, retireOrgApps, arg.FinalizedAt, arg.OrgID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const retireOrgDomains = `-- name: RetireOrgDomains :execrows
UPDATE ms_billing.app_custom_domains domain_row
SET removed_at = COALESCE(domain_row.removed_at, $1::timestamptz)
FROM ms_billing.apps app
WHERE domain_row.app_id = app.app_id
  AND app.owner_org_id = $2::uuid
  AND domain_row.removed_at IS NULL
`

type RetireOrgDomainsParams struct {
	FinalizedAt time.Time `json:"finalized_at"`
	OrgID       string    `json:"org_id"`
}

func (q *Queries) RetireOrgDomains(ctx context.Context, arg RetireOrgDomainsParams) (int64, error) {
	result, err := q.db.Exec(ctx, retireOrgDomains, arg.FinalizedAt, arg.OrgID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const retireOrgModuleTimers = `-- name: RetireOrgModuleTimers :execrows
UPDATE ms_billing.app_module_overage_timers timer
SET removed_at = COALESCE(timer.removed_at, $1::timestamptz)
FROM ms_billing.apps app
WHERE timer.app_id = app.app_id
  AND app.owner_org_id = $2::uuid
  AND timer.removed_at IS NULL
`

type RetireOrgModuleTimersParams struct {
	FinalizedAt time.Time `json:"finalized_at"`
	OrgID       string    `json:"org_id"`
}

func (q *Queries) RetireOrgModuleTimers(ctx context.Context, arg RetireOrgModuleTimersParams) (int64, error) {
	result, err := q.db.Exec(ctx, retireOrgModuleTimers, arg.FinalizedAt, arg.OrgID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const retireOrgPaymentMethods = `-- name: RetireOrgPaymentMethods :execrows
UPDATE ms_billing.payment_methods_mirror payment_method
SET deleted_at = COALESCE(payment_method.deleted_at, $1::timestamptz),
    is_default = false
FROM ms_billing.accounts account
WHERE payment_method.account_id = account.id
  AND account.owner_kind = 'org'
  AND account.owner_org_id = $2::uuid
  AND payment_method.deleted_at IS NULL
`

type RetireOrgPaymentMethodsParams struct {
	FinalizedAt time.Time `json:"finalized_at"`
	OrgID       string    `json:"org_id"`
}

// Funding methods are retained as soft-deleted audit rows.  Sponsor-owned
// cards are on the sponsor's user account and are intentionally untouched.
func (q *Queries) RetireOrgPaymentMethods(ctx context.Context, arg RetireOrgPaymentMethodsParams) (int64, error) {
	result, err := q.db.Exec(ctx, retireOrgPaymentMethods, arg.FinalizedAt, arg.OrgID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const stripeFundingAuthorization = `-- name: StripeFundingAuthorization :one

SELECT funding_auth.account_id,
       funding_auth.generation,
       funding_auth.funding_account_id,
       COALESCE(funding.stripe_customer_id, '')::text AS stripe_customer_id,
       EXISTS (
           SELECT 1
           FROM ms_billing.payment_methods_mirror payment_method
           WHERE payment_method.account_id = funding_auth.funding_account_id
             AND payment_method.deleted_at IS NULL
             AND (payment_method.exp_year, payment_method.exp_month) >= (
                 EXTRACT(YEAR FROM current_date)::INT,
                 EXTRACT(MONTH FROM current_date)::INT
             )
       ) AS has_usable_payment_method
FROM ms_billing.account_funding_authorizations funding_auth
JOIN ms_billing.accounts funding
  ON funding.id = funding_auth.funding_account_id
WHERE funding_auth.account_id = $1::uuid
`

type StripeFundingAuthorizationRow struct {
	AccountID              string `json:"account_id"`
	Generation             string `json:"generation"`
	FundingAccountID       string `json:"funding_account_id"`
	StripeCustomerID       string `json:"stripe_customer_id"`
	HasUsablePaymentMethod bool   `json:"has_usable_payment_method"`
}

// Durable organization billing retirement (migration 052).
// StripeFundingAuthorization returns the rotating current funding authority
// plus the PM/customer facts needed to arm a durable attempt. Callers persist
// generation + funder in the same transaction before any Stripe request.
func (q *Queries) StripeFundingAuthorization(ctx context.Context, accountID string) (StripeFundingAuthorizationRow, error) {
	row := q.db.QueryRow(ctx, stripeFundingAuthorization, accountID)
	var i StripeFundingAuthorizationRow
	err := row.Scan(
		&i.AccountID,
		&i.Generation,
		&i.FundingAccountID,
		&i.StripeCustomerID,
		&i.HasUsablePaymentMethod,
	)
	return i, err
}
